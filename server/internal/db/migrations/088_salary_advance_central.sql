-- +goose Up
-- +goose StatementBegin
--
-- 088_salary_advance_central — авансы сотрудникам филиалов из кассы центра
-- (Ф-С5, продолжение Ф-Р). Зеркало аванса на филиале обязано создавать
-- строку salary_advances — кап периода вычитает авансы ИМЕННО из неё
-- (advDedForPeriod), а не из финопов: без строки аванс, выданный центром,
-- не уменьшал бы остаток «к выплате» и открывал бы двойную выплату.
--
-- У такой строки нет счёта: деньги списаны на ЦЕНТРЕ, его счета на филиал
-- не реплицируются (Ф5 возит счета вверх, не вниз). Снимаем NOT NULL;
-- NULL = «выдан другим узлом сети» — локальная отмена такого аванса
-- запрещена гвардом (возвращать деньги здесь некуда), отмена — на центре.
ALTER TABLE salary_advances ALTER COLUMN account_id DROP NOT NULL;
-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
-- Возврат NOT NULL невозможен, пока существуют строки центра (account_id
-- NULL) — down оставлен no-op сознательно.
SELECT 1;
-- +goose StatementEnd
