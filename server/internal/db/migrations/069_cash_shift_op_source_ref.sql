-- +goose Up
-- +goose StatementBegin
--
-- 069_cash_shift_op_source_ref — регресс БАГ #28: авто-зеркало (__auto_mirror__)
-- реального оттока (зарплата/возврат/расход) можно было удалить как обычную
-- операцию смены — expected_cash расходился со счётом, хотя деньги реально
-- ушли (см. TestAudit_DeleteAutoMirror_KeepsShiftConsistent).
--
-- Разрешение на удаление __auto_mirror__ (v3.16.163/165) задумывалось СТРОГО
-- для фантомных зеркал — созданных старым багом recordShiftCashOutIfActive
-- (до v3.16.162) не в ту смену, обнаруженных владельцем только после закрытия.
-- Для ТАКИХ зеркал реверс баланса — no-op, потому что счёт эта запись никогда
-- не дебетовала (деньги списаны исходной ДДС-операцией в другом месте).
--
-- Но у категории __auto_mirror__ не было способа отличить фантомное зеркало
-- от РЕАЛЬНОГО (созданного recordShiftCashOutIfActive ПРЯМО СЕЙЧАС для
-- реальной, ещё действующей проводки) — удаление реального зеркала точно так
-- же не трогало баланс счёта (справедливо для фантома), но при этом теряло
-- единственную запись, которой смена знала об этом оттоке — expected_cash
-- переставал видеть реально потраченные деньги.
--
-- source_ref — id financial_operations, отток которой отражает это зеркало.
-- NULL — legacy/фантомные зеркала (в т.ч. созданные до этой миграции) — для
-- них поведение не меняется. Заполнен И указанная запись ещё существует —
-- зеркало реальное и действующее, удалять напрямую нельзя (см.
-- DeleteOperation/DeleteExpense): чтобы отменить его влияние на кассу, нужно
-- сторнировать/отменить ИСХОДНУЮ операцию, а не просто убрать зеркало.
ALTER TABLE cash_shift_operations
  ADD COLUMN IF NOT EXISTS source_ref TEXT;
-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
ALTER TABLE cash_shift_operations DROP COLUMN IF EXISTS source_ref;
-- +goose StatementEnd
